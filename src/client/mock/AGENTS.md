# mock/ — 规则层

继承根规则，见 [../../../AGENTS.md](../../../AGENTS.md)。

mock/ 特有约束：

- 金额必须是字符串，且与后端契约同形（八位小数），不许图省事写成数字。
- 每个场景只覆写自己关心的字段，其余走 `make()` 的默认值。
- 任何读取都必须容错：localStorage 在隐私模式下会抛，URL 解析也可能失败，都要静默回落。
- 这里不许出现真实网络请求。
